收到，感谢您非常具体和直观的反馈！这些问题都直击用户体验痛点。我将立即进行以下修复和优化：

### **1. 修复大屏战报 (404 Error)**
*   **问题**：`dashboard/index.html` 文件缺失或路径配置错误。
*   **解决**：
    *   检查 `static` 目录结构，确保 `dashboard/index.html` 存在。
    *   如果文件在 `templates` 下，URL 应该走 Django View 渲染而不是直接访问 static。
    *   **Action**：创建一个新的 View `DashboardView` 指向 `dashboard/index.html`，并修改 `jazzmin_settings` 中的 URL 为 `/api/dashboard/screen/`（或类似），不再直接链接静态文件。

### **2. 优化 "AI 智能添加任务" 按钮样式 (UI)**
*   **问题**：按钮样式简陋，像“毛坯”。
*   **解决**：
    *   使用 Bootstrap/Jazzmin 的原生按钮类（`btn btn-primary` 或 `btn-info`），增加 padding、圆角和阴影。
    *   移除行内样式，使用 CSS class。

### **3. 重构 AI 任务生成逻辑 (交互体验)**
*   **问题**：跳转新页面且界面简陋；直接创建没有确认环节。
*   **解决**：
    *   **交互升级**：改为在 `change_list` 页面**右上角点击后弹窗**（Modal）或者跳转到一个更美观的页面。
    *   **逻辑升级**：点击“解析”后，**不要直接创建**，而是跳转到（或回填到）标准的 `add_view` 页面，并预填充解析出的 `title`、`deadline`、`description`。
    *   **所见即所得**：用户可以在保存前修改解析结果。

### **4. 全局品牌替换**
*   **问题**：品牌名称不一致。
*   **解决**：全量搜索 `石榴经营`，替换为 **`石榴粒粒`**。

### **5. 工作报告 AI 增强 & 导航重命名**
*   **问题**：导航名称不准确；缺少 AI 辅助生成功能。
*   **解决**：
    *   **重命名**：修改 `WorkReport` 的 `verbose_name` 为“工作报告”。
    *   **AI 字段**：在 `WorkReport` 中增加 `ai_raw_input` 字段（“AI 原始指令”），并实现 `save_model` 逻辑或前端 JS 逻辑，将输入内容解析填充到 `content` 字段。

### **6. 赛事管理按钮重命名**
*   **问题**：按钮名称啰嗦。
*   **解决**：修改 `Competition` 的 `verbose_name` 为“赛事信息”（这样按钮会自动变为“增加赛事信息”）。

---

## 🚀 执行步骤

1.  **品牌替换**：全局搜索替换“石榴经营” -> “石榴粒粒”。
2.  **模型/导航修正**：
    *   修改 `core/models.py`：`WorkReport` 名称、`Competition` 名称。
    *   修改 `WorkReport` 模型，增加 `ai_raw_input` 用于 AI 生成。
3.  **大屏修复**：
    *   确认 `dashboard/index.html` 位置。
    *   在 `core/views.py` 增加 `screen_view`。
    *   更新 `jazzmin_settings.py` 的 URL。
4.  **AI 待办交互重构**：
    *   修改 `admin/core/todotask/change_list.html`：优化按钮样式。
    *   修改 `admin/core/todotask/add_by_ai.html`（或废弃）：改为点击解析后，**重定向到** `admin/core/todotask/add/?title=...&description=...`，利用 GET 参数预填充表单。这是最符合 Django Admin 逻辑的“确认”流程！

Let's do it!