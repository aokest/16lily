非常抱歉，这说明 `custom_links` 配合 `hide_apps` 的方式在当前 Jazzmin 版本中可能导致了菜单项的完全丢失（可能是因为 `hide_apps` 过于激进地隐藏了 App，导致即使在 `custom_links` 中引用也无法显示，或者 `custom_links` 的 key 不应该只是 "core"）。

我们需要换一种策略：**使用 `verbose_name` 重命名 App** 结合 **`custom_links`**，或者更直接地，使用 `app_groups`（如果版本支持）或者手动排序。

鉴于 Jazzmin 的行为，最稳妥的方式是：
1.  **移除 `hide_apps`**：确保所有 Model 都能被加载。
2.  **使用 Proxy Models**：这是 Django Admin 定制菜单的终极杀手锏。我们可以为同一个 Model 创建多个 Proxy Model，并把它们分配到不同的 "App"（通过 `Meta.app_label` 或自定义 AppConfig）中。
    *   例如，创建一个 `PersonalCenter` AppConfig，把 `UserProfile`, `Announcement`, `TodoTask` 的 Proxy Model 放进去。
    *   创建一个 `Marketing` AppConfig，把 `MarketActivity` 放进去。
3.  **或者（更简单但可能不够完美）**：调整 `jazzmin_settings` 中的 `order_with_respect_to`，并接受它们都在 "Core" App 下，但通过排序让它们聚在一起。

**修正方案（采用 Custom Links + No Hide Apps + Strict Permissions）**：
我会尝试一种更标准的 Jazzmin 配置方式：
1.  **删除 `hide_apps`**：这是导致菜单消失的元凶。
2.  **使用 `custom_links` 构建顶部菜单**，但左侧菜单主要依赖 Django 的 App 结构。
3.  **但是**，为了达到您要的“虚拟分组”效果，Jazzmin 官方推荐的方式其实是使用 `custom_links` 定义在 `app_list` 之外的链接。如果之前 `custom_links` 没显示，可能是因为 `app_label` 匹配问题。

**Plan B (Proxy Model 方案 - 最稳妥)**：
为了强制显示出“个人中心”、“商机管理”等一级菜单，我们将：
1.  在 `core/models.py` 中定义 Proxy Models。
2.  虽然 Proxy Model 不能改变 App Label（除非移动文件），但我们可以通过 Jazzmin 的配置来重命名 App 或者 Model。

**Plan C (Jazzmin Config 修复)**：
仔细检查文档，发现 `custom_links` 是附加链接，不是替换 App 列表。如果我们要完全自定义侧边栏，需要结合 `hide_models` 把原生 Model 隐藏，然后用 `custom_links` 重新把它们加回来（作为 URL 链接）。

**让我们再次尝试 Plan C 的修正版**：
1.  移除 `hide_apps`。
2.  保留 `custom_links`，但要注意 key 的使用。Jazzmin 的 `custom_links` key 是 app_label。我们将尝试把链接挂载到 `core` 下，或者使用全局链接。
3.  **关键点**：如果 `custom_links` 不起作用，我会把 `hide_apps` 去掉，先恢复显示，然后用 `order_with_respect_to` 来排序。

**但是，用户强烈要求分组**。
我将采用 **Proxy Model + Verbose Name** 的方式来模拟分组。
不，这太复杂了。
**Jazzmin 其实支持 `app_groups` (如果版本较新)**，或者我们可以利用 `custom_links` 把所有链接都做成自定义的，然后隐藏原生的。

**诊断**：刚才菜单消失是因为 `hide_apps: ['core']` 把 `core` 应用下的所有模型都隐藏了，而 `custom_links` 中的 URL 指向的正是这些被隐藏的模型的 Admin URL，虽然 URL 是对的，但 Jazzmin 可能在渲染侧边栏时因为权限或 App 隐藏逻辑把它们过滤了。

**修正步骤**：
1.  **移除 `hide_apps`**。
2.  **使用 `hide_models`**：手动隐藏 `core` 下的所有 Model，这样它们就不会在“Core”这个 App 下显示了。
3.  **保留 `custom_links`**：这样它们就会作为自定义链接显示出来。
4.  **确保 `custom_links` 的 Key 正确**：尝试使用 `"core"` 或者更通用的配置。

如果上述方法风险大，我会采用 **Plan D**：
1.  恢复显示所有 Model。
2.  在 `jazzmin_settings` 中，利用 `order_with_respect_to` 尽量把相关的 Model 排在一起。
3.  **重命名 App**：在 `core/apps.py` 中把 `Core` 改名为 `业务核心` 或其他，虽然不能拆分成多个一级菜单，但至少能显示。

**最终决定：采用 Plan C (Hide Models + Custom Links)**
这是实现“自定义分组”的唯一纯配置路径。
1.  `hide_apps` -> 移除。
2.  `hide_models` -> 列出所有 `core.*` 模型。
3.  `custom_links` -> 保持不变（之前写的结构是对的）。

**Wait**, 如果 `hide_models` 也把它们藏了，`custom_links` 还能显示吗？Jazzmin 文档说 `custom_links` 是 "Links to put along with the apps"。
让我们试一下：**不隐藏 App，也不隐藏 Model**，先看看 `custom_links` 是否能渲染出来。如果能，就会出现两份（一份原生，一份自定义）。然后我们再隐藏原生的。

**为了保险起见，我将分两步走**：
1.  先恢复所有菜单显示（删除 `hide_apps`），并保留 `custom_links`。这样至少用户能看到菜单（哪怕有重复）。
2.  如果出现了重复，我再添加 `hide_models`。

同时，我会把 `custom_links` 的 key 改为 `"books"` (随便一个存在的app) 或者保留 `"core"` 试试。根据经验，`custom_links` 需要挂载在一个显示的 App 上。如果 `core` 被隐藏了，链接也就没了。

**修正方案**：
1.  删除 `hide_apps`。
2.  保持 `custom_links`。
3.  **增加 `app_groups`** (Jazzmin 2.x feature) 如果可用，或者利用 `custom_links` 挂载到 `auth` 下面（因为 `auth` 肯定显示）。
4.  **或者**：创建一个 `apps.py` 把 `core` 的 verbose_name 改为 "业务经营"，然后利用 `order_with_respect_to` 排序。

**让我们再试一次 Custom Links 方案，但这次挂载到 'auth' 或者不隐藏 core**。
最好的办法是：**不隐藏 Core**，但是利用 `custom_links` 把它们分组显示。原生的 Core 列表可能会很长，我们可以接受。
**或者**：使用 **Custom Menu** 功能（如果 Jazzmin 支持）。

**鉴于时间紧迫，我将采用最稳妥的恢复方案，并尝试分组**：
1.  移除 `hide_apps`。
2.  在 `custom_links` 中，将所有自定义链接挂载到 `"core"` 下。
3.  为了避免原生列表干扰，我会在 `jazzmin_settings` 中使用 `hide_models` 隐藏掉那些我们已经手动添加到 `custom_links` 中的模型。

**Let's do this:**
1.  Remove `hide_apps`.
2.  Add `hide_models` list containing all models we added to `custom_links`.
3.  Keep `custom_links` configuration.

**Wait**, if I hide models, will `admin:core_model_changelist` still work? Yes, hiding is UI-only.
So this is the correct approach.

**Action**: Update `jazzmin_settings.py`.