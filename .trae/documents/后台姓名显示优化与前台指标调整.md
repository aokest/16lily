已完成以下修改，请您查阅：

## 1. 后台显示真实姓名
现在后台的**商机列表**、**跟进记录**等关键位置，凡是涉及到人员（如负责销售、操作人）的地方，都优先显示用户的**真实姓名**（Last Name + First Name）。
- 如果用户未设置真实姓名，系统会自动降级显示用户名。
- 该列支持点击表头进行排序（基于First Name）。

## 2. 前台战报移除“合同毛利”
前台大屏仪表盘（Dashboard）已移除“合同毛利”卡片，现在只保留与业绩考核直接相关的三个核心指标：
- **新签合同额** (Signed)
- **回款毛利** (Return Profit)
- **确认收入** (Revenue)
界面布局已自动调整为三列均分，视觉效果更加平衡。

## 3. 关于“一直未结束的命令”
您提到的终端命令 `Terminal#287-490`（即 `python manage.py runserver`）是**Web服务器的主进程**。
- **它的作用**：它必须一直运行，您的后台管理系统和前台大屏才能被访问。如果它结束了，网站就打不开了。
- **状态**：它会实时输出访问日志（如您看到的 GET /admin/...），这是正常现象，表明系统正在健康工作。

您现在可以刷新后台和前台页面查看最新效果。