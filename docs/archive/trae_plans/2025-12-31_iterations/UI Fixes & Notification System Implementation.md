收到，我将把全局通知功能加入计划。以下是更新后的完整实施计划：

### 1. CardEditor.vue (卡片编辑器) 修复
*   **“输出物”框布局锁定**：
    *   通过 CSS Flexbox (`flex-col`, `flex-1`, `h-full`) 强制右侧栏的“输出物”区域自动填充所有剩余垂直空间。
    *   确保其**下边框**严格对齐左侧栏底部，**上边框**位置保持正常流布局。
*   **“样式”按钮可见性修复**：
    *   排查 CSS 遮挡或层级问题 (`z-index`)，确保按钮在工具栏中正常显示并可点击。

### 2. MainLayout.vue (全局导航栏) 通知功能开发
*   **红点状态同步**：
    *   实时读取消息状态（基于 `localStorage`），只要存在“未读”消息，铃铛图标显示红色圆点；所有消息已读则红点消失。
*   **消息下拉列表**：
    *   点击铃铛弹出下拉菜单。
    *   展示最近 10 条消息（按时间倒序）。
    *   底部添加“更多通知消息”链接，点击跳转至个人中心。

待您确认后，我将立即执行这些修改。